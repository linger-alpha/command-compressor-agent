Run the following shell command exactly once so its complete output is returned to you:

```sh
perl -e 'for (1..120) { print "Downloading package $_% 1MB/s\n" }'
```

After you have seen the command result, create `/app/hook-probe-complete`.
